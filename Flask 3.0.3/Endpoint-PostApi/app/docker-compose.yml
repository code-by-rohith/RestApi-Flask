version: '3.8'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db  

  flask-app:
    build: .
    ports:
      - "5005:5000"
    depends_on:
      - mongodb

volumes:
  mongodb_data: